spring:
  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true

  security:
    csrf:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      probes:
        enabled: true
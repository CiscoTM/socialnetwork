spring:
  datasource:
    url: jdbc:tc:postgresql:16:///socialnetwork
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  test:
    database:
      replace: none

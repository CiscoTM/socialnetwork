spring:
  application:
    name: socialnetwork
  datasource:
    url: jdbc:postgresql://localhost:5433/backend_social_network
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org:
      hibernate.SQL: DEBUG